extends ../layout.pug

block appContent
        h1 you are now in the food category
        p You can choose the goods down below
        - const length = (Object.keys(database.food)).length
                
        - for (let x = 0; x < length; x++)
            br
            a(href=`/products/${x+1}`) id:#{x+1} #{Object.keys(database.food)[x]}
            br 